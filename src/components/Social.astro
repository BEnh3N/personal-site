---
const { platform, username } = Astro.props;
---

<a
    class="rounded bg-secondary text-text font-display py-1 px-2 capitalize hover:-translate-y-0.5"
    href={`https://www.${platform}.com/${username}`}>{platform}</a
>
