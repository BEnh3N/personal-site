---
const { platform, username } = Astro.props;
---

<a
  class="rounded bg-secondary px-2 py-1 font-display capitalize text-text hover:-translate-y-0.5"
  href={`https://www.${platform}.com/${username}`}>{platform}</a
>
