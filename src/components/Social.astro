---
const { platform, username } = Astro.props;
---

<a
  class="rounded-md bg-accent px-2 py-1 font-display font-bold text-background active:scale-95"
  href={`https://www.${platform}.com/${username}`}>{platform}</a
>
