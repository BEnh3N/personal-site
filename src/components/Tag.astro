---
const { tag } = Astro.props;
---

<a
  href={`/tags/${tag}`}
  class="bg-accent-button hover:bg-accent-hover text-display shadow-border rounded-md px-4 py-2 text-sm active:scale-95"
  >{tag}</a
>
